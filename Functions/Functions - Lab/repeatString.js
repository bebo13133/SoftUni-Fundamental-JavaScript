function repeat(text, n) {
    return text.repeat(n);
  } 
  repeat('abc', 3);

  /*
Вариант 2 
function repeatString(text, num){
let textResult = "";
for(let i = 0 ; i<num ; i++){
    textResult+=text
   
}
return textResult
}

  */